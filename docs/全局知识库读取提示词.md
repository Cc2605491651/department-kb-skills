# 全局知识库读取提示词

把下面代码块复制到当前Agent每次都会读取的全局规则文件，例如 `AGENTS.md` 或 `CLAUDE.md`，然后替换知识库链接。

```md
## 公司知识库读取规则

蒸馏后Wiki：<粘贴蒸馏后Wiki链接>

只有用户明确要求查询公司知识库、提供钉钉文档链接，或者问题必须依靠公司内部资料才能准确回答时，才启用本规则。普通聊天、写作、编程和通用知识问答不启用，也不执行DWS命令。

启用后，先读取已安装的department-kb-distill Skill中的《07-蒸馏后Codex问答与DWS检索规范》，按规范执行：

1. 使用DWS搜索候选文档，并过滤掉不属于当前蒸馏后Wiki的结果。
2. 使用 `dws doc block list --node "<nodeId>" --content-format jsonml --start-index 0 --end-index 1 --format json` 读取候选文档页首的17字段和蒸馏画像。
3. 按 `stable_id` 合并同一文档的不同目录视图。
4. 再读取相关文档的完整原文。简单问题通常读取1～3份；跨流程、对比、汇总或冲突问题按需分批扩展，直到关键信息完整，不得因数量限制遗漏依据。
5. 页首信息只用于筛选，数字、日期、制度、责任人和具体结论必须以完整原文为准。

回答时附文档名称、状态、更新时间和原文链接。发现不同文档存在冲突时，列出双方说法，不要自行判断哪份有效。没有找到可靠依据时直接说明，不得用常识补充公司内部事实。

默认只读。未经用户在当前任务中明确允许，不得创建、修改、移动、删除、发布文档或改变权限。
```

这段规则只在查询公司知识库时启用，不会要求Agent在普通交流中查询钉钉。
