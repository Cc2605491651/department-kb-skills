# 部门知识库Skills使用说明

这套工具包含两个Skill：

- `department-kb-distill`：第一次完整蒸馏知识库。
- `department-kb-maintain`：首次蒸馏上线后，定期检查新增和修改内容。

## 第一步：安装

```bash
git clone https://github.com/Cc2605491651/department-kb-skills.git
cd department-kb-skills
python3 install.py --target "<个人Skill目录>" --mode link
```

不知道个人Skill目录在哪里时，把下面这句话发给AI：

```text
请确认你当前使用的Agent个人Skill目录，然后运行本仓库的install.py安装两个Skill。使用软链接方式安装；安装后检查是否识别成功，需要重启就直接告诉我。
```

## 第二步：首次全量蒸馏

把下面内容发给AI：

```text
请使用department-kb-distill处理这个知识库。

原钉钉知识库链接：
<粘贴链接>

蒸馏后Wiki文件夹链接：
<粘贴链接>

先只生成本地结果，不要上传。任务完成后用简单的话告诉我：处理了多少份、失败多少份、需要我检查什么、下一步做什么。任务开始后我可以离开，不需要持续等待。
```

完成后重点检查AI在对话中给出的：

- 成功、失败和排除数量；
- 失败文件清单；
- 需要负责人确认的内容；
- 本地审核入口和结果矩阵；
- 下一步必须完成的动作。

## 第三步：确认并上线

确认本地结果没有问题后，把下面内容发给AI：

```text
我已经检查本地蒸馏结果，确认此次蒸馏没有问题。请将蒸馏后的最终Wiki上线到之前提供的钉钉文件夹，并执行完整回读检查。完成后告诉我发布数量、失败数量、回读结果和仍需处理的问题。
```

只有用户明确确认后，AI才能把本批文档状态从“候选”改为“正式”并上线。

## 第四步：设置增量维护

首次蒸馏验收并上线后，把下面内容发给AI：

```text
请使用department-kb-maintain，为这个知识库设置定期检查。

每个工作日上午9点检查一次，只处理新增或修改的文档。每次建立一个带日期的新结果文件夹，保留以前的检查记录，不要自动上传。

原知识库链接：
<粘贴链接>

最近一次已经确认的本地结果目录：
<粘贴目录>

所有增量结果统一保存到：
<粘贴总文件夹目录>

每次完成后告诉我：新增或修改了多少份、哪些文件失败、哪些内容需要负责人确认、我下一步做什么。如果上一次结果还没有确认，先提醒我，不要覆盖，也不要自动上线。
```

## 获取Skill更新

在本仓库目录执行：

```bash
git pull --ff-only
```

使用软链接安装时，拉取完成后新版本会直接生效。Agent没有识别到变化时，重启Agent。

使用复制模式安装时，重新执行：

```bash
python3 install.py --target "<个人Skill目录>" --mode copy --replace
```

旧版本会自动备份，不会直接删除。
